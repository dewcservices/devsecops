# Example Python Application
Very simple python code with some tests utilising PyTest. Code is designed to be tested with the PyTest CI Workflow via the repositories Github Actions.

## Config
devsecops/pyproject.toml points 'pythonpath' at this directory to allow the tests to correctly import the application python files.